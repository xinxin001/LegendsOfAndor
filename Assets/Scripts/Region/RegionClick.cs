using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RegionClick : MonoBehaviour
{
    public GameObject region;

    // Update is called once per frame
    void Update()
    {
        var mouseClick = Input.GetMouseButtonUp(0);
        if (mouseClick)
        {
            
        }
    }
}
